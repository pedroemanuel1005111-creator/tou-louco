// StackBlitz auto-init script for Quiz Digital
// Para usar no StackBlitz: abra um novo projeto Node.js e execute:
// node import.meta.js

const fs = require('fs');
const path = require('path');

const projectRoot = __dirname;

console.log('🚀 Inicializando projeto Quiz Digital...');
console.log('📁 Criando estrutura de pastas...');

// Lista de arquivos para criar
const files = [
  // ... aqui virá a lista completa de arquivos
];

console.log('✅ Estrutura criada!');
console.log('📦 Execute: npm install && npx drizzle-kit push && npm run dev');
